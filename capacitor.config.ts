import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primetv.app',
  appName: 'PrimeTV',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
